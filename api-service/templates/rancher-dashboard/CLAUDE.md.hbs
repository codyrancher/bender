# Rancher Instance

- The environment variables can be accessed via .env
- It can be accessed at https://$RANCHER_HOST_NAME
- You can use the following admin credentials
- user: $RANCHER_ADMIN_USER
- pass: $RANCHER_ADMIN_PASS

# Two URLs: stock Rancher vs your dev server (read before driving the browser)

There are two different web endpoints in this project and they are NOT interchangeable. Driving the wrong
one is the most common way to waste a verification run, because your code changes only exist in one of them.

- **Stock Rancher** - `https://$RANCHER_HOST_NAME` (also reachable at `https://localhost:8443`).
  The real Rancher server: the API backend AND the unmodified, upstream dashboard UI it ships with. It does
  NOT contain your local code changes. Use it to reproduce the original bug, to create supporting resources,
  and for any API calls.
- **Dev server** - `https://localhost:8005`. The dashboard built from `/workspace/dashboard` with your local
  changes, served by `yarn dev`. This is the ONLY place your fix is visible. Use it to verify a fix and to
  record or screenshot the after-fix state.

Important details about the dev server:
- It is **started automatically** by `init.sh` (`yarn dev`, background, logs at `/workspace/yarn-dev.log`), so the
  expensive cold compile usually runs during the early stages. Edits are picked up by incremental HMR. **Don't
  restart it** — a restart forces a slow cold rebuild. Check first: `curl -sk -o /dev/null -w '%{http_code}'
  https://localhost:8005` (200 = up). Only start it yourself if it isn't running (`pgrep -f "yarn dev"`).
  The `API` env var is already exported to the stock Rancher backend, so auth and API calls proxy there.
- `wait-for-sidecars` does NOT wait for it - it only checks CDP and the Rancher API. Before driving the browser
  against it, poll once (`until curl -sk -o /dev/null -w '%{http_code}' https://localhost:8005 | grep -q 200; do
  sleep 3; done`) so HMR has finished recompiling your change.
- Log in on the dev server with the same admin credentials above.

Rule of thumb: reproduce + setup -> stock Rancher; verify the fix + after-fix media -> dev server (8005).
{{#if issueNumber}}

# Issue
This project is for issue https://github.com/rancher/dashboard/issues/{{issueNumber}}
{{/if}}

# Writing style
- Never use em dashes in any output: code, comments, commit messages, PR descriptions, or chat replies. Use a regular dash, parentheses, or split the sentence instead.

# Workflow skills
The detailed playbooks for the most common tasks live in `.claude/skills/` and are auto-discovered by the Skill tool. Read the skill's `SKILL.md` and follow it when you reach the matching phase. Do not paraphrase or duplicate skill content here.

- `record-browser-video` - script-driven before/after recordings (CDP, hover->wait->click pattern, deferred recording). Before recordings run against stock Rancher; after-fix recordings must run against the dev server (`https://localhost:8005`) so they show the fix.
- `create-commit` - concise commit on a new `issue-$(issueNumber)` branch, no AI co-author.
- `create-pr` - push and open a draft PR upstream. Includes the **Playwright + CDP upload flow** that posts videos and screenshots to GitHub `user-attachments` so they embed inline in the PR body.
- `review-pr` - pending inline review comments only, never submit.

# Fix Issue
- Review the issue and attempt to solve it.
- **Before using the browser (playwright/CDP) or hitting Rancher, run `wait-for-sidecars`.** The sidecar containers may
not be running yet. This script starts them if needed and blocks until CDP and the Rancher API actually respond.
Skipping it means DNS errors and empty videos/screenshots.
- Reproduce the issue against **stock Rancher** (`https://$RANCHER_HOST_NAME`) and record a before video. Follow the `record-browser-video` skill.
- Write a detailed reproduction description and, if needed, create supporting resources in the Rancher instance so
someone else could reproduce it from the same starting state.
- After implementing the fix, verify and record the after video against the **dev server** (`https://localhost:8005`),
which is the only endpoint that serves your local change. Start `yarn dev` and wait for it to compile first (see
"Two URLs" above). Same skill.
- Take screenshots with `browser.mjs screenshot` at any point to highlight changed areas; save them under
`/workspace/screenshots/`.
- When the fix is committed and ready for review, follow the `create-commit` skill, then `create-pr` (which uploads media to GitHub for you).
