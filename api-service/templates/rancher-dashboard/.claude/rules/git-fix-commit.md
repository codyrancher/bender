# git-fix-commit

After a commit, run `git-fix-commit` to amend the most recent commit's message. It opens the message in your editor (`$EDITOR`, else nano) — save and close to apply. No args — operates on `HEAD`.
